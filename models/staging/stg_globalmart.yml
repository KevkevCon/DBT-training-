version: 2 

models:
  - name: stg_orders
    describtion: one unique order per row
    columns: 
      - name: orderid
        discription: the primary key for stg_orders
      - name: shipmode
        description: '{{doc("shipmode")}}'